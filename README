Example of Setup

cd ~/src
git clone --recursive https://github.com/sogaiu/ts-grammar-checker
cd ts-grammar-checker
jpm deps

Example of Use

# assuming tree-sitter-clojure's shared object is "installed"
cd ~/src/ts-grammar-checker
janet clojure-checker.janet ../clojars-cljish # path to dir tree of clojure files
